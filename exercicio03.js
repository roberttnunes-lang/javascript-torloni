let numero = parseInt(prompt("Digite um numero para saber se e par ou impar:"));

if (numero % 2 === 0) {
    alert("O número " + numero + " é PAR.");
} else {
    alert("O número " + numero + " é ÍMPAR.");
}